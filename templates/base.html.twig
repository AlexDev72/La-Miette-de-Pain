<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}{% endblock %}
			| La Miette de Pain</title>

		<!-- CSS -->
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/cookieconsent@3/build/cookieconsent.min.css">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=BIZ+UDMincho&family=Kalam:wght@300;400;700&display=swap">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
		<link rel="stylesheet" href="/asset/style.css">
		<link
		rel="stylesheet" href="/asset/page.css">

		<!-- Favicon -->
		<link rel="icon" type="image/png" sizes="16x16" href="/slider/fb.png">

		{% block stylesheets %}
			{# {{ encore_entry_link_tags('app') }} #}
		{% endblock %}
	</head>
	<body>
		 {% block header %}
    <header class="position-relative">
        <div class="container d-flex justify-content-between align-items-center py-3">
            <!-- Logo/Titre -->
            <div class="d-flex align-items-center">
                <img class="logo me-3" width="60" height="60" src="/image/fb.png" alt="Logo La Miette de Pain">
                <h1 class="entete m-0">
                    <a href="{{ path('app_index') }}" class="text-decoration-none">La Miette de Pain</a>
                </h1>
            </div>

            <!-- Boutons Admin/Déconnexion -->
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="admin-buttons">
                {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin') }}" class="btn btn-admin me-2">
                    <i class="fas fa-cog me-1"></i> Admin
                </a>
                {% endif %}
                <a href="{{ path('app_logout') }}" class="btn btn-logout">
                    <i class="fas fa-sign-out-alt me-1"></i> Déconnexion
                </a>
            </div>
            {% endif %}
        </div>
   

			<nav class="navbar navbar-expand-lg navbar-light bg-white py-2" style="border-bottom: 3px solid #89d006;">
				<div
					class="container-fluid px-4">
					<!-- Bouton toggle pour mobile -->
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Menu">
						<span class="navbar-toggler-icon"></span>
					</button>

					<!-- Contenu de la navbar -->
					<div class="collapse navbar-collapse" id="navbarContent">
						<ul
							class="navbar-nav w-100 d-flex justify-content-around">
							<!-- Menu Produits -->
							<li class="nav-item dropdown flex-grow-1 text-center">
								<a class="nav-link dropdown-toggle px-3" href="#" id="produitsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									<i class="fas fa-bread-slice me-2"></i>Produits
								</a>
								<ul class="dropdown-menu w-100 animate slideIn" aria-labelledby="produitsDropdown">
									<li>
										<a class="dropdown-item" href="{{ path('patisserie_liste') }}">Pâtisserie</a>
									</li>
									<li><hr class="dropdown-divider"></li>
									<li>
										<a class="dropdown-item" href="{{ path('viennoiserie_liste') }}">Viennoiserie</a>
									</li>
									<li><hr class="dropdown-divider"></li>
									<li>
										<a class="dropdown-item" href="{{ path('traiteur_liste') }}">Traiteur</a>
									</li>
								</ul>
							</li>

							<!-- Menu Événements -->
							<li class="nav-item dropdown flex-grow-1 text-center">
								<a class="nav-link dropdown-toggle px-3" href="#" id="eventsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									<i class="fas fa-calendar-alt me-2"></i>Événements
								</a>
								<ul class="dropdown-menu w-100 animate slideIn" aria-labelledby="eventsDropdown">
									<li>
										<a class="dropdown-item" href="{{ path('fetes_liste') }}">Fêtes</a>
									</li>
									<li><hr class="dropdown-divider"></li>
									<li>
										<a class="dropdown-item" href="{{ path('evenement_liste') }}">Mariage/Baptême/Anniversaire</a>
									</li>
								</ul>
							</li>

							<!-- Lien À propos -->
							<li class="nav-item flex-grow-1 text-center">
								<a class="nav-link px-3" href="{{ path('app_apropos') }}">
									<i class="fas fa-info-circle me-2"></i>À propos
								</a>
							</li>

							<!-- Lien Contact -->
							<li class="nav-item flex-grow-1 text-center">
								<a class="nav-link px-3" href="{{ path('app_contact') }}">
									<i class="fas fa-envelope me-2"></i>Contact
								</a>
							</li>
						</ul>
					</div>
				</div>
			</nav>
 </header>
 
			<style>
				.nav-link {
					color: black !important;
				}

				@media(min-width: 768px) {
					.animate {
						animation-duration: 0.3s;
						-webkit-animation-duration: 0.3s;
						animation-fill-mode: both;
						-webkit-animation-fill-mode: both;
					}
				}

				@keyframes slideIn {
					0% {
						transform: translateY(1rem);
						opacity: 0;
					}
					100% {
						transform: translateY(0rem);
						opacity: 1;
					}
				}

				@-webkit-keyframes slideIn {
					0% {
						-webkit-transform: transform;
						-webkit-opacity: 0;
					}
					100% {
						-webkit-transform: translateY(0);
						-webkit-opacity: 1;
					}
				}

				.slideIn {
					-webkit-animation-name: slideIn;
					animation-name: slideIn;
				}

				.navun {
					background-color: #fff;
					border-bottom: solid 3px rgb(180, 215, 57);
				}
			</style>
		{% endblock %}

		{% block body %}{% endblock %}

		<footer>
			<div class="grid-container">
				<div class="grid-item">
					<p class="texte3">
						<a href="{{ path('app_contact') }}">Nous contacter</a><br>
						Pour plus d'information et pour suivre toutes les actualités<br>de La Miette de Pain
					</p>
				</div>

				<div class="grid-item">
					<h5>Catégories</h5><br>
					<a href="{{ path('patisserie_liste') }}">Pâtisserie</a><br>
					<a href="{{ path('viennoiserie_liste') }}">Viennoiserie</a><br>
					<a href="{{ path('traiteur_liste') }}">Traiteur</a>
				</div>

				<div>
					<p>
						<i class="fab fa-facebook-square"></i>
						<a target="_blank" href="https://www.facebook.com/maisonbourgine">Facebook</a><br>
						<i class="fab fa-instagram"></i>
					</p>
				</div>

				<div>
					<a href="{{ path('rgpd') }}">Mentions légales</a>
				</div>
			</div>
		</footer>

		<!-- JavaScript -->
		<script src="https://cdn.jsdelivr.net/npm/cookieconsent@3/build/cookieconsent.min.js" data-cfasync="false"></script>
		<script>
			window.cookieconsent.initialise({
"palette": {
"popup": {
"background": "#efefef",
"text": "#404040"
},
"button": {
"background": "#8ec760",
"text": "#ffffff"
}
},
"content": {
"message": "Politique de confidentialité\nLe site web lamiettedepain.fr est détenu par La Miette de Pain, qui est un contrôleur de données de vos données personnelles.\n\nNous avons adopté cette politique de confidentialité, qui détermine la manière dont nous traitons les informations collectées par lamiettedepain",
"dismiss": "Acceptez",
"link": "En savoir plus"
}
});
		</script>

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
		<script src="/asset/js.js"></script>

		{% block javascripts %}
		{# {% block importmap %}{{ importmap('app') }}{% endblock %}
    {{ encore_entry_script_tags('app') }} #}{% endblock %}
	</body>
</html>
