{% set viennoiserie_show = path('viennoiserie_show', {'viennoiserie_show' : viennoiserie}) %}


{% block body %}
    <div>
        <div>

            <img width= 300px height=250px src="upload/images/Patisserie/{{ viennoiserie.image }}">

            <h2>{{ viennoiserie.name }}</h2>

        </div>
    </div>
{% endblock %}
